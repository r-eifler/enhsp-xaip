(define (problem test) (:domain additivedomain)
(:init
    ;todo: put the initial state's facts and numeric values here
)

(:goal (and
    ;todo: put the goal condition here
    (a)
    (b)
    (c)
    (d)
    (e)
    (f)
    
))

;un-comment the following line if metric is needed
;(:metric minimize (???))
)
