:;Header and description

(define (domain domain_name)

(:functions ;todo: define numeric functions here
    (x)
)

;define actions here
(:action a
    :parameters ()
    :precondition (and )
    :effect (and (assign (x) 0))
)
(:action b
    :parameters ()
    :precondition (and )
    :effect (and (increase (x) 1))
)


)