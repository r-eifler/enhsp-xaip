;; in this problem we expect to find the goal landmark plus b and a which was the only way to find b
(define 
    (problem instance1)
    (:domain testing)
    (:init
	;(a)
	(= (counter) 0)
    )
    (:goal 
	    (and
           	(c)
	    )
    )
)
